// pointLogs.js
// Legacy stub to avoid 404 errors when old references remain.
// The original point logs admin UI was removed, but some environments
// still attempt to load this module during login, leading to a missing
// resource. These no-op exports keep the app functional if referenced.

export function setPointLogsData() {}
export function renderPointLogs() {}
export function setupPointLogFilters() {}
